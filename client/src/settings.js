// game geometry must match the settings in the API
// (an improvement: client asks the API for geometry when connecting...)

module.exports = Object.freeze({
  HALF_SIZE_X: 16,
  HALF_SIZE_Y: 12,
  DEFAULT_API_LOCATION: 'ws://localhost:8000'
})
